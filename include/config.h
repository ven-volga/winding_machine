#pragma once

// =============================================================
// config.h — всі налаштування проєкту в одному місці
// =============================================================

// ─────────────────────────────────────────
// МОТОРИ
// ─────────────────────────────────────────

#define MOTOR_STEPS     200   // кроків на оберт NEMA17
#define MICROSTEPS      16    // мікрокроки TMC2209 (MS1/MS2)

// ─────────────────────────────────────────
// КІНЕМАТИКА
// ─────────────────────────────────────────

#define SPINDLE_RATIO   2.0f  // передача ременя шпинделя 1:2
#define T8_LEAD_MM      2.0f  // крок гвинта T8 (мм/оберт)

// ─────────────────────────────────────────
// РОЗГІН
// ─────────────────────────────────────────

#define ACCEL_START_DELAY_US  8000  // початкова затримка (мкс)
#define ACCEL_STEP_US         10    // крок зміни затримки

// ─────────────────────────────────────────
// ПОЗИЦІЮВАННЯ КАРЕТКИ
//
// Фіксована повільна швидкість для точного переміщення
// до заданої позиції (Set Home, секційна намотка).
// Менша швидкість = точніше позиціювання.
// ─────────────────────────────────────────

#define CARRIAGE_HOME_SPEED_RPM  50   // об/хв при переміщенні до позиції

// ─────────────────────────────────────────
// ЕНКОДЕР
// ─────────────────────────────────────────

#define ENCODER_STEP_MS   2     // мінімальний інтервал між кроками
#define LONG_PRESS_MS     800   // час довгого натиску (мс)
#define BTN_DEBOUNCE_MS   30    // debounce кнопки (мс)

// Час утримання для Set Home (мс)
// 5 секунд — захист від випадкового скидання
#define SET_HOME_HOLD_MS  5000

// ─────────────────────────────────────────
// ДІАПАЗОНИ ПАРАМЕТРІВ UI
// ─────────────────────────────────────────

#define SPINDLE_SPEED_MIN   10
#define SPINDLE_SPEED_MAX   600
#define SPINDLE_SPEED_STEP  10

#define WIRE_DIAMETER_MIN   0.05f
#define WIRE_DIAMETER_MAX   3.00f
#define WIRE_DIAMETER_STEP  0.01f

#define WINDING_WIDTH_MIN   1.0f
#define WINDING_WIDTH_MAX   200.0f
#define WINDING_WIDTH_STEP  0.5f

// Діапазон ручного задання позиції каретки
#define CARRIAGE_POS_MIN    0.0f
#define CARRIAGE_POS_MAX    200.0f
#define CARRIAGE_POS_STEP   0.5f    // крок зміни в UI (мм)